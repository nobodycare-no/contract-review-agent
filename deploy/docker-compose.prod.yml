# 生产 override（V2 双容器：mysql + app；mock 容器已随统一系统废除）
# 用法：docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
services:
  app:
    restart: unless-stopped

  mysql:
    restart: unless-stopped

volumes:
  attachment_data:
